import { colors as colors_colors } from "./colors";
import { condense as condense_condense } from "./condense";
import { indent as indent_indent } from "./indent";
import { rpad as rpad_rpad } from "./rpad";
import { sum as sum_sum } from "./sum";
module.exports = {
    colors: colors_colors,
    condense: condense_condense,
    indent: indent_indent,
    rpad: rpad_rpad,
    sum: sum_sum
};
export { indexjs_indexjs as indexjs };
